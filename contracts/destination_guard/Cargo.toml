[workspace]

[package]
name        = "destination-guard"
version     = "0.1.0"
edition     = "2021"
description = "Interface-level Stellar destination address guard helpers. Reject empty and wrong-length inputs before deeper StrKey parsing."
license     = "MIT"
publish     = false

[lib]
crate-type = ["rlib"]
path = "src/lib.rs"

[dependencies]
